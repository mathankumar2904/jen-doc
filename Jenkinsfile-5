pipeline {
    agent any

    stages {
        stage('Test Node Version inside Docker') {
            steps {
                bat """
                docker run --rm node:16-alpine node --version
                """
            }
        }
    }
}
