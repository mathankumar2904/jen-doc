pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                bat """
                docker build -t my-app .
                docker run --rm my-app node --version
                docker run --rm my-app git --version
                docker run --rm my-app curl --version
                """
            }
        }
    }
}
